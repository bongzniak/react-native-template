import themeData from '../design-token.json';

const LightTheme = {
  always: themeData.user.always,
  current: themeData.user.light,
  picker: themeData.user.picker,
  auto: themeData.user.auto,
  pop: themeData.user.pop,
};

export default LightTheme;
